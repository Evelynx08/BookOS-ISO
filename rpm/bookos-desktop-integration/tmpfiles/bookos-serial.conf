# The "About this PC" widget (bookos-menu) reads the machine serial from DMI.
# The kernel exposes it root-only (0400); relax to world-readable at boot so
# the widget can show it without pkexec (same info macOS shows in About).
z /sys/devices/virtual/dmi/id/product_serial 0444 root root - -
